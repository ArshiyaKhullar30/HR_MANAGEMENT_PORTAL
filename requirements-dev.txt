# Enterprise HR AI — development dependencies
-r requirements.txt

# --- testing ---
pytest>=8.0,<9.0
pytest-cov>=5.0,<8.0
httpx>=0.27,<1.0            # FastAPI TestClient transport

# --- code quality ---
black>=24.3,<26.0
ruff>=0.4,<1.0
pre-commit>=3.7,<5.0
nbstripout>=0.7,<1.0        # MANDATORY: keeps employee PII out of git history

# --- notebooks ---
jupyterlab>=4.1,<5.0
ipykernel>=6.29,<7.0
