# Enterprise HR AI — runtime dependencies
# Direct dependencies declared with compatible ranges.
# The fully-resolved environment is locked in requirements.lock.txt (pip freeze).
# Regenerate the lock with:  make lock

# --- core data ---
pandas>=2.2,<3.0
numpy>=1.26,<3.0
pyarrow>=15.0            # Parquet engine — the working data format
scipy>=1.11,<2.0

# --- validation / contracts ---
pandera>=0.20,<0.25
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0

# --- machine learning ---
scikit-learn>=1.4,<2.0
xgboost>=2.0,<4.0
shap>=0.45,<1.0
joblib>=1.4,<2.0

# --- experiment tracking ---
mlflow>=2.12,<4.0

# --- semantic matching (role crosswalk + recommendation engine v2) ---
sentence-transformers>=3.0,<6.0

# --- api ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- frontend ---
streamlit>=1.35,<2.0
plotly>=5.20,<7.0
matplotlib>=3.8,<4.0

# --- monitoring ---
evidently>=0.4,<1.0

# --- config / runtime ---
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0

# --- platform note (macOS) ---------------------------------------------
# XGBoost on macOS needs the OpenMP runtime, which pip does not provide:
#   conda install -c conda-forge llvm-openmp     (or: brew install libomp)
# Without it `import xgboost` raises XGBoostError: libomp.dylib not loaded.
# Handled by `make setup`.
