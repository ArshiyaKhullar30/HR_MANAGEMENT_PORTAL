"""Config for the API layer — re-exported so there is one implementation."""

from hrai.utils.config import (  # noqa: F401
    data_dir,
    dataset_meta,
    get,
    get_config,
    project_root,
    raw_path,
    seed,
)
