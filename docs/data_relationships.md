# Data Relationships

> Generated by `python -m hrai.profiling.relationships` (Step 04).
> Generated: `2026-08-27T20:38:49.903633+00:00`

## Topology

```
  POPULATION A                                  POPULATION B
  employee_attrition (1,470)                    hr_performance_engagement (3,000)
  key:   EmployeeNumber                         key:   Employee ID
  label: Attrition           16.1% Yes          label: Voluntarily Terminated  10.8%
        |                                                     |
        |  X-------- NO EMPLOYEE-LEVEL JOIN (F1) --------X    |
        |                                                     |
        | JobRole (9)                              Title (31) |
        v                                                     v
        +--------->  ROLE -> O*NET SOC CROSSWALK  <-----------+
                     semantic + human-reviewed, 40 roles
                                  |
                                  v
                        occupation_master (1,016 SOC)
                          |                        |
                          v                        v
              essential_skills              software_skills
              TIER 1 foundational           TIER 2 technical
              10 skills, IM 1-5 / LV 1-7    8,751 tools / 134 categories
              910 occupations               923 occupations
```

The two employee populations are bridged **by the role/skill ontology, not by employee identity**. See `docs/adr/001-two-population-architecture.md`.

## Every pair of tables

| Left | Right | Join key | Cardinality | Referential integrity | Why |
|---|---|---|---|---|---|
| `employee_attrition` | `hr_performance_engagement` | `EmployeeNumber` = `Employee ID` | **NONE — forbidden** | n/a | Coincidental overlap: 753 shared IDs, attributes agree 27.3%. Different companies (F1). |
| `employee_attrition` | `occupation_master` | `JobRole` -> `soc_code` via crosswalk | many-to-one | via crosswalk | 0 exact title matches (F4); resolved by the semantic crosswalk. |
| `hr_performance_engagement` | `occupation_master` | `Title` -> `soc_code` via crosswalk | many-to-one | via crosswalk | 0 exact title matches (F4); resolved by the semantic crosswalk. |
| `occupation_master` | `essential_skills_processed` | `soc_code` | one-to-many | PASS | Tier 1 foundational skills. 106 occupations have no skills row. |
| `occupation_master` | `software_skills_processed` | `soc_code` | one-to-many | PASS | Tier 2 technical tools. 93 occupations have no tools row. |
| `engagement_processed` | `engagement_events_processed` | `employee_id` | one-to-many | PASS | Employee grain vs event grain (F5): 3,000 employees, 3,150 events. |

## The forbidden join, in full

Build Notes Step 04 states the two employee tables join one-to-one on `EmployeeID`. Tested before building:

| Attribute compared | Rows compared | Agree | Agreement |
|---|---|---|---|
| `Gender vs GenderCode` | 753 | 366 | **48.6%** |
| `Age vs _age_from_dob` | 753 | 45 | **6.0%** |

> COINCIDENTAL OVERLAP — attributes agree only 27.3% on the 753 shared key values. These are different populations; joining them would fabricate records.

Enforced by `scripts/check_no_cross_population_join.py` in pre-commit and CI, and asserted by `tests/unit/test_no_cross_population_join.py`.

## Population summary

| Population | Table | Employees | Label | Positive rate |
|---|---|---|---|---|
| A | `employee_attrition_processed` | 1,470 | `attrition_flag` | 16.1% |
| B | `engagement_processed` | 3,000 | `is_voluntary_exit` | 10.7% |

Population B's independent label is what makes cross-population transfer validation possible: a model trained on A can be scored against B's own outcomes rather than against an assumption.
